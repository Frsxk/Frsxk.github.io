{
  "name": "frxskie-portfolio",
  "version": "1.0.0",
  "description": "Personal portfolio website",
  "scripts": {
    "dev": "npx tailwindcss -i ./input.css -o ./styles.css --watch",
    "build": "npx tailwindcss -i ./input.css -o ./styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
